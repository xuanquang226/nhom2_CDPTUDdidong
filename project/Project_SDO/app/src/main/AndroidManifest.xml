<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quang.project_sdo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SellerManagementActivity"></activity>
        <activity android:name=".EnteringDrugActivity">

        </activity>
        <activity android:name=".SalesStatisticsActivity" />
        <activity android:name=".BackStackActivity"></activity>
        <activity android:name=".ArcticleDetailActivity"></activity>
        <activity android:name=".SignInSignUpActivity">
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </activity>
        <activity android:name=".SignInActivity"></activity>
        <activity android:name=".SignUpActivity"><intent-filter>

        </intent-filter></activity>
    </application>

</manifest>